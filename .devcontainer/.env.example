# Copy this file to .env and adjust values for your local dev environment.
# Loaded into the WordPress container at startup (see docker-compose.yml).

# Domain used by ElaiaPagesMethods when WP_DEBUG is true.
# Overrides shortcode attribute and HTTP_HOST auto-detection.
ELAIA_DEV_DOMAIN=mondomaine-de-test.fr